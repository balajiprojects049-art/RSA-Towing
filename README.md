# RSA Towing Prototype

Quick prototype built with Next.js + Tailwind CSS + Framer Motion.

Setup:

```bash
cd "C:/Users/hp/OneDrive/Desktop/staffarc/RSA Towing/site"
npm install
npm run dev
```

Open http://localhost:3000

Notes:
- Replace placeholders (images, Google Maps embed) before production.
- Add environment variables and analytics as needed.
